global:
  resolve_timeout: 1m
route:
  receiver: 'default'
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h
receivers:
  - name: 'default'
    webhook_configs:
      - url: 'http://host.docker.internal:3999/alerts'
        send_resolved: true
